if ('favorite document for user exists in database') {
    if ('the id in my route param is NOT in the database array') {
        'push it in!'
    } else {
        'tell the user it is already there'
    }
} else {
    'make a new favorite collection for the user'
}